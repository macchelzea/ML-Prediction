# ML-Prediction
Running some predictive analysis.

Ensure that all necessary packages are installed. 

## To run the code:
```bash
conda create -n venvMLOps python=3.12 -y

conda activate venvMLOps

pip install -r requirements.txt
```
